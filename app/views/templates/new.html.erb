<h1>新しいテンプレートの作成</h1>

<%= form_with(model: @template, local: true) do |form| %>
  <% if @template.errors.any? %>
    <div style="color: red;">
      <h2><%= @template.errors.count %>個のエラーがあります:</h2>
      <ul>
        <% @template.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form_label :title, "テンプレート名（例：会社、ジム、旅行）" %><br>
    <%= form.text_field :title, required: true %>
  </div>

  <div class="field">
    <%= form.label :memo, "メモ（任意）" %><br>
    <%= form.text_area :memo %>
  </div>

  <hr>
  <h3>持ち物リスト（最大3つまでテスト入力可能）</h3>

  <%= form.fields_for :items do |item_form| %>
    <div class="field" style="margin-bottom: 10px;">
      <%= item_form.label :name, "持ち物名" %>
      <%= item_form.text_field :name %>
    </div>
  <% end %>
  <hr>

  <div class="actions">
    <%= form.submit "テンプレートを保存する" %>
  </div>
<% end %>

<%= link_to "戻る", root_path %>